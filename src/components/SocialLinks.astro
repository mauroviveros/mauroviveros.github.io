---
import { SOCIALS } from "@consts";
import SocialLink from "./SocialLink.astro";
---
<article class="flex flex-wrap gap-1 items-center justify-center">
    {
        SOCIALS.map((SOCIAL) => (
          <SocialLink social={SOCIAL}/>
        ))
      }
</article>